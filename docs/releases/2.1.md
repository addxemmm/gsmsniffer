# 2.1 — Single complete image / 单一完整镜像

## Changes / 变更

- One Docker Hub tag: **`addxemmm/gsmsniffer:2.1`**. The image includes gr-gsm/tshark; demo and shielded modes do not require separate images.
- Frontend and same-origin API: `18083`; API-only backend: `8083`. Compose binds both host ports to loopback by default.
- Default mode remains `demo`, with no USB mapping, no automatic jobs and `restart: "no"` (no boot startup).
- The optional shielded Compose override changes runtime mode, permissions and state isolation, not the image tag.
- Source revision labels, image digest and provenance replace extra commit-qualified tags. The [2.0.0 record](2.0.0.md) remains historical.
- The default scanning workbench now follows scan frequencies → select a returned channel → explicitly start IMSI or SMS capture. Capture parameters come from the selected scan result, not a manually entered frequency. Results and the active-task stop control remain on the same page.
- The API exposes `/api/v1/frequencies`; new capture requests require `scan_job_id` and a matching selectable frequency from the current runtime mode. Legacy history remains readable but unlinked old observations are not capture inputs: run a new scan after upgrading.

镜像统一为2.1，依赖完整但不自动启用硬件。前端18083、后端8083，服务不开机自启，Token与数据卷升级时保留。

## Scope / 使用限制

**仅限自有测试SIM、自有终端，在有效屏蔽室或屏蔽箱内使用。**
**Use only owned test SIMs and terminals inside an effectively RF-shielded room or enclosure.**

Identities remain permanently masked and SMS output remains event-only without plaintext bodies. Installed dependencies and HTTP health checks do not prove physical shielding, SDR compatibility or successful RF reception. Physical RF acceptance is a separate laboratory procedure and is not claimed by this release description.

## Original verification record / 初次发布验证记录

Verified on 2026-09-09:

- Source: `31559561d5999d57f40c396a3e9ac1220c60f68c`.
- [CI passed](https://github.com/addxemmm/gsmsniffer/actions/runs/34320930065): Go tests, race/vet, publication checks, complete image build and RF-free smoke.
- [Container publication passed](https://github.com/addxemmm/gsmsniffer/actions/runs/34320930423).
- `addxemmm/gsmsniffer:2.1` digest: `sha256:7fd3dac69102f5553cef8ea6df4b45ed08204713c2b818ae46e1d99d1521af2c`.
- Server pulled that digest. Version/revision, both health/readiness endpoints, authentication gates, UI/API isolation, preserved token/data volume and restart policy `no` passed. Mode remains `demo`, with no active job. Dependency executables were located without running RF operations.
- [Historical tag cleanup passed](https://github.com/addxemmm/gsmsniffer/actions/runs/34321769811). An independent anonymous Docker Hub inventory confirmed exactly one tag: `2.1`, with the same digest.
- Only the unused previous gsmsniffer image was removed from the deployment host. Other services and data volumes were preserved.

No physical RF tests were performed. Dependency availability and HTTP health checks are not hardware acceptance.

See [deployment](../DEPLOY.md), [release checklist](../RELEASING.md) and [testing](../TESTING.md).

## Optional-authentication update / 可选鉴权更新

Current 2.1 source adds anonymous access when no token file is configured and the environment token is unset/empty. The UI discovers the mode from public `/api/v1/auth` and connects automatically without a token in anonymous mode. Valid configured credentials require login; malformed configured credentials fail startup. The optional `compose.auth.yml` overlay retains file-based login. Anonymous mode grants every reachable client management and data access; restrict it to trusted LAN clients. The earlier acceptance evidence above records its original authenticated deployment and is not evidence for these later changes.


### Optional-auth deployment verification / 可选鉴权部署验证

Verified on 2026-09-09 after the operator explicitly confirmed anonymous LAN management access:

- Current application source: `2f7612b7fbc79bf5546d71086eb508f02162c32f`.
- [Release workflow passed](https://github.com/addxemmm/gsmsniffer/actions/runs/34325244266), including both anonymous and token-authenticated container smoke checks, frontend auto-connect tests, Go/race tests, and invalid credential-file failure checks.
- The sole `2.1` image tag now resolves to `sha256:ec0aaac09fe9d358004ddfcdece294e2fe576c8072cadcb11cec98bb46e897fb`; older digests above are historical.
- Both deployed LAN ports return `required: false` from `/api/v1/auth` and allow status requests without an Authorization header. Cross-origin requests still return 403.
- Browser verification showed the anonymous-mode notice, automatic Connected status, no token form, version2.1 and no active task. No RF task was started.
- Host bindings remain `0.0.0.0:18083` and `0.0.0.0:8083`; container health is healthy and restart policy is `no`. Existing data volume is preserved. The previous token file is retained privately but no longer configured or mounted.

## Scan-first workflow update / 先扫频再采集更新

Verified on 2026-09-09:

- Application source: `445818587a8a3140e8aa03e8b248934305f2e2c1`.
- [Release workflow passed](https://github.com/addxemmm/gsmsniffer/actions/runs/34327906549): Go tests/race/vet, scan-selection UI workflow tests, optional-auth and transport tests, publication checks, complete image build and anonymous/authenticated DEMO container smoke.
- Docker Hub inventory contains only `2.1`, now resolving to `sha256:3cd7b9b6d5a50d40680ef8b1bf70df4f6ab5385952940d1b337b2dffa508cc16`. Earlier digests and source revisions above are historical evidence, not the current mutable tag.
- A local browser DEMO test completed scan → select frequency → IMSI capture → SMS capture on the same frequency. Selection alone starts nothing; unavailable frequencies and active tasks disable conflicting controls. IMSI was masked and SMS text redacted.
- The deployment host pulled this exact image and serves the new revision on both LAN ports. Health/readiness, anonymous auth metadata, status and frequency catalog returned successfully without credentials. The deployed browser opened directly to the connected scan workbench; no frequency was selected and capture was disabled until a new scan provides selectable results.
- Existing pre-update history remains readable. The initial catalog is empty because old observations lack scan linkage; this is not a migration failure. Run a new scan to populate selectable channels.

These checks used synthetic DEMO data and read-only deployed-service checks. No physical RF reception, IMSI reception or SMS reception test was performed.
